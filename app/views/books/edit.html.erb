<div class="book">
  <h1>Editing Book</h1>

  <% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <il><%= message %></il>
      <% end %>
    </ul>
  <% end %>

  <%= form_with model: @book, url: book_path(@book.id), methood: :patch do |f| %>

    <span class="form"><label>title</label></span>
    <%= f.text_field :title %>

    <span class="form"><label>body</label></span>
    <%= f.text_area :body %>

    <span class="field"><%= f.submit 'Update Book' %></span>

  <% end %>

  <%= link_to "Show", book_path(@book.id) %>
  |
  <a href="/books">Back</a>
</div>